///
/// HybridSMSSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `SMSResponse` to properly resolve imports.
namespace margelo::nitro::nitrosms { enum class SMSResponse; }
// Forward declaration of `SMSOptions` to properly resolve imports.
namespace margelo::nitro::nitrosms { struct SMSOptions; }

#include <NitroModules/Promise.hpp>
#include "SMSResponse.hpp"
#include <string>
#include <vector>
#include "SMSOptions.hpp"
#include <optional>

namespace margelo::nitro::nitrosms {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SMS`
   * Inherit this class to create instances of `HybridSMSSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSMS: public HybridSMSSpec {
   * public:
   *   HybridSMS(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSMSSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSMSSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSMSSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> isAvailable() = 0;
      virtual std::shared_ptr<Promise<SMSResponse>> sendSMS(const std::vector<std::string>& numbers, const std::string& message, const std::optional<SMSOptions>& options) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SMS";
  };

} // namespace margelo::nitro::nitrosms
