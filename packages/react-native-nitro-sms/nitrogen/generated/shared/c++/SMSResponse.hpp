///
/// SMSResponse.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrosms {

  /**
   * An enum which can be represented as a JavaScript union (SMSResponse).
   */
  enum class SMSResponse {
    UNKNOWN      SWIFT_NAME(unknown) = 0,
    SENT      SWIFT_NAME(sent) = 1,
    CANCELLED      SWIFT_NAME(cancelled) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrosms

namespace margelo::nitro {

  // C++ SMSResponse <> JS SMSResponse (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrosms::SMSResponse> final {
    static inline margelo::nitro::nitrosms::SMSResponse fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"): return margelo::nitro::nitrosms::SMSResponse::UNKNOWN;
        case hashString("sent"): return margelo::nitro::nitrosms::SMSResponse::SENT;
        case hashString("cancelled"): return margelo::nitro::nitrosms::SMSResponse::CANCELLED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum SMSResponse - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrosms::SMSResponse arg) {
      switch (arg) {
        case margelo::nitro::nitrosms::SMSResponse::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        case margelo::nitro::nitrosms::SMSResponse::SENT: return JSIConverter<std::string>::toJSI(runtime, "sent");
        case margelo::nitro::nitrosms::SMSResponse::CANCELLED: return JSIConverter<std::string>::toJSI(runtime, "cancelled");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert SMSResponse to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"):
        case hashString("sent"):
        case hashString("cancelled"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
